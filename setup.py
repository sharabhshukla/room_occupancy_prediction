from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This projects uses room occupancy model to predict occupancy in a room given a subset of features',
    author='Sharabh Shukla',
    license='MIT',
)
